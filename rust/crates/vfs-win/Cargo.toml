[package]
name = "vfs-win"
version = "0.1.0"
edition = "2021"

[dependencies]
vfs-env = { path = "../vfs-env" }
vfs-ipc = { path = "../vfs-ipc" }
windows-sys = { version = "0.61", features = [
  "Win32_Foundation",
  "Win32_Security",
  "Win32_System_Memory",
  "Win32_System_Threading",
] }
