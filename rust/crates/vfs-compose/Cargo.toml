[package]
name = "vfs-compose"
version = "0.1.0"
edition = "2021"
publish = false
description = "Read-only composition: layered (top-wins) + router (glob routes)"

[lib]
name = "vfs_compose"
path = "src/lib.rs"

[dependencies]
vfs-provider = { path = "../vfs-provider" }
