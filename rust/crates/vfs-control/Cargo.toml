[package]
name = "vfs-control"
version = "0.1.0"
edition = "2021"
publish = false
description = "Director control-plane gRPC contract + declarative config schema + client"

[lib]
name = "vfs_control"
path = "src/lib.rs"

[dependencies]
tonic = "0.12"
prost = "0.13"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
thiserror = "1"

[build-dependencies]
tonic-build = "0.12"
protoc-bin-vendored = "3"
