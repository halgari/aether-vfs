[package]
name = "vfs-shim-dll"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
vfs-env = { path = "../vfs-env" }
vfs-shim = { path = "../vfs-shim" }
windows-sys = { version = "0.61", features = ["Win32_Foundation"] }
