[package]
name = "vfs-cache"
version = "0.1.0"
edition = "2021"
publish = false
description = "Block cache (RAM LRU + optional disk) for director sources"

[lib]
name = "vfs_cache"
path = "src/lib.rs"

[dependencies]
vfs-provider = { path = "../vfs-provider" }
