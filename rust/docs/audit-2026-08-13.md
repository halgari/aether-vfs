# Code audit — 2026-08-13

Full-tree review following the removal of process hollowing. Covers dead code,
lint health, panic surface, test coverage, configuration surface and build
hygiene.

**Tree state:** 328 tests pass. `cargo build --workspace --all-targets` and
`cargo clippy --workspace --all-targets` are both clean of errors and
dead-code warnings.

**Status key:** ✅ fixed · ⬜ open, deferred to a later batch. §2.1, §2.2 and
§2.3 are done; the rest are recorded and deliberately untouched so each batch
stayed reviewable.

---

## 1. What is healthy

Worth stating, because these are deliberate properties that a reviewer should
not undo by accident.

- **The shim never panics.** `vfs-shim` has **zero** `unwrap`/`expect`/`panic!`
  in production code. That matters more than anywhere else in the tree: the shim
  runs inside the game's address space, so a panic is a game crash with no
  diagnostic. The two crates it calls into (`vfs-shared::reader`,
  `vfs-redirect::parse_full_dir_info`) do use `unwrap`, but every instance is
  provably infallible from a bounds or magic check a few lines above.
- **No TODO / FIXME / HACK / XXX anywhere** in the tree.
- **`unsafe` is concentrated where it must be** — `vfs-shim` (117),
  `vfs-inject` (34), `vfs-win` (32), `vfs-ipc` (30), `vfs-payload` (22) — and
  the pure crates have essentially none. `vfs-ipc` confines all of it to one
  segment accessor; `vfs-win` to mapping and events.
- **The pure layers are genuinely pure.** `vfs-core`, `vfs-redirect` and
  `vfs-shared` carry 44 / 41 / 23 tests against ~1k LOC each and need no OS, no
  game and no filesystem to exercise.

---

## 2. Findings

Ordered by what I would fix first.

### 2.1 ✅ FIXED — `vfs-payload` had no tests, across 497 LOC of the most delicate code in the tree

The zero-import pre-init payload runs **before `LdrpInitializeProcess`**, where
only ntdll and the EXE image are mapped. It hooks four path/attribute stubs and
takes every address it needs through a config struct because it cannot import
anything. It is the one component whose failure mode is "the process dies during
bootstrap with no logging".

It was also the only substantial crate with **zero** `#[test]`.

Much of it is testable off-target: the suffix match, the redirect-table walk and
the emitted machine code are pure. The reflective-map and RIP-redirect halves are
not, but `vfs-inject/tests/dual_layer.rs` covers the composed path.

**Fixed** in `667f568`. It had no tests because it *could not*: `no_std` plus a
custom `#[panic_handler]` and hand-written `memcpy`/`memset` conflict with a std
harness. Resolved with `#![cfg_attr(not(test), no_std)]`, `#[cfg(not(test))]` on
the freestanding glue, and — the actual blocker — `build.rs` emitting
`rustc-cdylib-link-arg` instead of `rustc-link-arg`, which had been applying
`/NODEFAULTLIB /ENTRY:DllMain` to the test binary too.

13 tests now cover suffix matching (including the component-boundary rule that
stops `api.dll` redirecting `steam_api.dll`), redirect-table bounds, the emitted
jump/trampoline machine code byte-for-byte, degenerate pointer inputs, and the
`Config` layout — mirrored in `vfs-inject::payload_cfg`, since that struct is
written by one crate and read by another that cannot share the type. The shipped
`vfs_payload.dll` is byte-identical (8704 bytes). Each load-bearing assertion was
mutation-checked.

Coverage against size, after the fix. `vfs-launch` is now the thinnest, but it
is a CLI over tested libraries rather than bootstrap code, so it does not carry
the same risk:

| crate | LOC | tests before | tests now |
|---|---:|---:|---:|
| vfs-payload | 497 | **0** | **13** |
| vfs-launch | 1157 | 1 | 1 |
| vfs-directord | 2232 | 14 | 14 |
| vfs-inject | 2329 | 15 | 16 |

### 2.2 ✅ FIXED — `cargo build --all-targets` could not build this tree

Nine output-filename collisions, then `LNK1104`. Cargo emits the cdylib of
`vfs-shim-dll` and `vfs-fixture-vproxy` once as a lib unit and again as a test
dependency, both to the same path under `deps/`, and the second link fails.

Pre-existing (measured at 9 collisions before any of this week's work) and *not*
fixed by `test = false` — it is a cargo/cdylib interaction, not a manifest bug.

**Consequence:** the most natural CI invocation is the one that does not work.
Anyone adding a gate will reach for `--all-targets` first and conclude the tree
is broken.

**Fixed** in `05f8603`. The two units came from cdylibs that were also path
dependencies, and neither edge was load-bearing: `vfs-shim-dll` was a
dev-dependency of `vfs-inject` purely to order the build, which `tests/common`
already does itself; `vfs-fixture-staticimp` depended on `vfs-fixture-vproxy`
for the same reason, reinforced by a `use vproxy as _;` that forced an extra
rlib. Removing the first is subtraction; for the second the ordering is real, so
it moved into `tests/common` as an explicit first cargo pass.

`cargo clippy --workspace --all-targets` now reports zero errors — the obvious
CI invocation is the one that works.

### 2.3 ✅ FIXED — Configuration was 47 environment variables, 25 of them undocumented

Every switch is read straight from the environment with an inline default, and
there is no schema, no listing and no validation. Undocumented ones include
several that change behaviour materially:

- `VFS_ALLOW_DISK_FALLTHROUGH` — whether under-root misses fall through to the
  real directory. This is the isolation invariant; a stray setting silently
  un-seals the root.
- `VFS_DISK_ONLY_ROOT`, `VFS_REJECT_FUSE_SECTION`,
  `VFS_REJECT_FUSE_DATA_SECTION`, `VFS_LAZY_NO_VEH` — each disables a serving
  path.
- `VFS_KEEP_HOST_STEAM_API` — must agree with the shim's own copy of the same
  decision, and the code says so in a comment rather than in a type.

`skyrim-live` defensively `remove_var`s two of these at startup, which is
evidence the risk is already understood.

**Fixed** by the `vfs-env` crate: one constant per name, a table of all 47 with
kind and default, and typed accessors. Every production call site now reads
through it; the three standalone fixture binaries keep literals rather than take
a dependency for one constant, and the drift test covers them anyway.

The test is the part worth keeping: it scans every `.rs` file in the workspace
and fails if a `VFS_*` name is read that is not registered. It found a live bug
on its first run — the hollow refactor had renamed `VFS_HOLLOW_HOST` to
`VFS_LAUNCH_IMAGE` at the writer, and `vfs-shim`'s `stage_root_from_env` still
read the old name, so the staging-directory alias had silently been resolving to
nothing. It had not bitten only because the same refactor moved the child's
working directory to the managed root. After the fix a live run shows 627
staging-path entries where there had been none.

The two boolean parsing rules were also unified without flattening them:
`opt_in` (off unless explicitly enabled) and `opt_out` (on unless explicitly
disabled). Collapsing both into one rule would have made `ALLOW_DISK_FALLTHROUGH`
— the isolation invariant — enableable by writing `off`.

### 2.4 ⬜ OPEN — 11 hand-built NUL-terminated byte strings

Clippy flags `b"NtCreateFile\0"` and friends; Rust has had `c"NtCreateFile"`
since 1.77.

This is not cosmetic here. Two literal NUL **bytes** (rather than the two-char
escape) reached the tree during this week's refactor, and although they compiled
to identical bytes, they made git and grep classify `hook.rs` and `pe.rs` as
*binary* — silently disabling diff, blame and content search on the largest file
in the project. Several searches during the refactor returned "Binary file
matches" for that reason.

**Recommend:** convert to `c"..."` literals. The construct that allowed the bug
disappears.

### 2.5 ✅ FIXED (the instance) · ⬜ OPEN (the pattern) — test-isolation flake

`lazy_section::eager_sections_are_freed_on_close` failed roughly one run in
three: it asserts a specific VA base became `MEM_FREE`, and a freed base is
immediately available to a sibling test reserving on another thread. Fixed by
serialising the VA-touching tests.

Nothing about the product was racy; only the observation. But the same shape —
a test asserting on process-global state while peers run concurrently — exists
wherever tests touch the hook tables, the fuse-synth handle table or the stats
counters. `hookstats` already guards against this by having its tests avoid the
globals.

**Recommend:** when adding a test that asserts on a process-global, take the
same lock rather than assuming test order.

### 2.6 ⬜ OPEN — Hardcoded `C:\GameLayers` defaults remain in production paths

Most are in tests and fixtures, which is fine, but two are live defaults:

- `vfs-shim/src/fuse_client.rs`: managed root defaults to
  `C:\GameLayers\runtime` when `VFS_VIRTUAL_DIR` is unset.
- `vfs-launch/src/main.rs`: `DEFAULT_LAYERS = C:\GameLayers`.

The layout moved to `C:\tmp\skyrim-*` for the current harness, so these defaults
now point at a directory that does not exist on this machine. A missing
`VFS_VIRTUAL_DIR` therefore fails late and confusingly rather than immediately.

**Recommend:** make the root a required input in the shim's config rather than a
defaulted one; there is no sensible default for "which tree am I virtualising".

### 2.7 ⬜ OPEN — Duplicate dependency versions

`bitflags` 1.3 + 2.13, `getrandom` 0.2 + 0.3 + 0.4, `hashbrown` 0.12 + 0.17,
`indexmap` 1.9 + newer. All transitive, mostly via the gRPC/tonic stack.

Low priority — no correctness impact — but it inflates build time and binary
size for a project whose data plane is meant to be thin.

### 2.8 ⬜ OPEN — `vfs-server` is documented as legacy but still shipped

Its own module docs say "Production hosts use `vfs-director`", yet `vfs-launch`
still depends on it and it carries 786 LOC and 11 tests.

**Recommend:** decide. Either it is the bench/legacy path and should say so in
its `Cargo.toml` description, or it should be retired the way `ghostly` was.

### 2.9 ⬜ OPEN — 57 remaining clippy warnings

None are correctness issues. The largest groups: 11 nul-terminated strings
(§2.4), 6 redundant closures, 6 `sort_by` → `sort_by_key`, 5 manual
`is_multiple_of`, 4 `write!` with trailing newline, 4 "items after a test
module", 3 unannotated transmutes.

The three unannotated `transmute`s are worth doing by hand rather than with
`--fix`: they are the detour trampoline casts, where writing the types out is
genuine documentation of an ABI assumption.

---

## 3. Fixed during this audit

| Finding | Commit |
|---|---|
| §2.1 `vfs-payload` untested — now 13 tests + a mirrored ABI pin | `667f568` |
| §2.2 `--all-targets` unbuildable — duplicate cdylib build units | `05f8603` |
| §2.3 47 env switches, 25 undocumented — now `vfs-env` + a drift test, which found a live bug | pending |
| Dead code the hollow refactor left: `VFS_VIRTUAL_IMAGE` spoof subsystem (`VirtualImageScope`, `should_spoof_image_path`, GetModuleFileNameW/A and QueryFullProcessImageNameW detours), `IN_HOLLOW_CREATE`, `STATUS_NOT_IMPLEMENTED`, `Hook::Other`, a never-read `OpenKind::Dir { size }`, 5 unused imports, a `hollow_pe` key left in an e2e fixture's TOML | `0d02417` |
| Two literal NUL bytes making `hook.rs` / `pe.rs` binary to git and grep | `0d02417` |
| 46 double-encoded UTF-8 sequences in `hook.rs` comments (pre-existing) | `0d02417` |
| A hardcoded `C:\GameLayers\vfs-state\gmf-spoof.log` debug append on a hot hook | `0d02417` |
| Clippy failing the workspace: 19 `not_unsafe_ptr_arg_deref` errors on `inject_dll`, `arm_preinit_payload{,_ex}`, `install_late` | `59d15e8` |
| `lazy_section` VA test flake (~1 in 3) | `41e8e38` |

Each was verified against the running game — SKSE launch, masters loading,
`coc riverwood` into the world — not only against the test suite.

---

## 4. Open items, in the order I would take them

| # | Item | Why it matters | Rough size |
|---|---|---|---|
| §2.4 | 11 hand-built NUL-terminated strings | the construct that put two literal NUL bytes in the tree and turned the largest file binary to git | mechanical, `c"…"` literals |
| §2.6 | `C:\GameLayers` defaults in live paths | a missing `VFS_VIRTUAL_DIR` fails late and confusingly instead of immediately | small; make the root required |
| §2.8 | `vfs-server` legacy but shipped | 786 LOC of ambiguity — retire it or say it is the bench path | decision first, then small |
| §2.9 | 57 clippy warnings | none are correctness; 3 unannotated transmutes are worth doing by hand | mechanical except those 3 |
| §2.7 | duplicate dependency versions | build time and binary size only | transitive; low leverage |
| §2.5 | the test-isolation *pattern* | the instance is fixed; the shape recurs wherever a test asserts on process-global state | convention, not code |

## 5. Deliberately not changed

- **`unsafe` density in `vfs-shim`.** 117 occurrences is proportionate to what
  the crate does; the module docs already scope where it is allowed.
- **The remaining clippy warnings.** Mechanical, and applying `--fix` in bulk
  across a tree with this much `unsafe` would produce a diff nobody can review
  against the running game.
