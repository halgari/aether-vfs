[package]
name = "vfs-zip"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
vfs-core = { path = "../vfs-core" }
vfs-director = { path = "../vfs-director" }
vfs-protocol = { path = "../vfs-protocol" }
