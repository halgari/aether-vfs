[package]
name = "vfs-ops"
version = "0.1.0"
edition = "2021"
publish = false
description = "Backend ops trait for userspace FUSE (no OS, no zip)"

[dependencies]
vfs-protocol = { path = "../vfs-protocol" }
